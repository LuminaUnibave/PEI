<configuration>
    <timestamp key="currentDate" datePattern="yyyy-MM"/>
    <property name="LOG_PATH" value="C:\Workspace\Repositorys\PEI\logs"/>


    <!-- ==================== JSON LOGS ==================== -->
    <appender name="FILE_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/json/app-log-%d{yyyy-MM}.json</fileNamePattern>
            <maxHistory>12</maxHistory>
        </rollingPolicy>

        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <prettyPrint>true</prettyPrint>
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                    <pattern>yyyy-MM-dd'T'HH:mm:ss.SSSZ</pattern>
                </timestamp>
                <logLevel><fieldName>level</fieldName></logLevel>
                <loggerName><fieldName>logger</fieldName></loggerName>
                <threadName><fieldName>thread</fieldName></threadName>
                <message><fieldName>message</fieldName></message>
                <stackTrace><fieldName>stacktrace</fieldName></stackTrace>

                <mdc><fieldName>usuario</fieldName><mdcKey>usuario</mdcKey></mdc>
                <mdc><fieldName>requestId</fieldName><mdcKey>requestId</mdcKey></mdc>
                <mdc><fieldName>ip</fieldName><mdcKey>ip</mdcKey></mdc>
            </providers>
        </encoder>
    </appender>

    <!-- ==================== TXT LOGS COM CORES ANSI ==================== -->
    <appender name="FILE_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/txt/app-log-%d{yyyy-MM}.txt</fileNamePattern>
            <maxHistory>12</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ==================== NÍVEIS DE LOG ==================== -->

    <!-- SOMENTE SEU PACOTE PRINCIPAL -->
    <logger name="com.unibave.Lumina" level="INFO"/>

    <!-- DESATIVA TUDO O RESTANTE -->
    <logger name="org.springframework" level="OFF"/>
    <logger name="org.springframework.boot" level="OFF"/>
    <logger name="org.springframework.security" level="OFF"/>
    <logger name="org.hibernate" level="OFF"/>
    <logger name="org.apache" level="OFF"/>
    <logger name="org.flywaydb" level="OFF"/>
    <logger name="com.zaxxer.hikari" level="OFF"/>
    <logger name="org.postgresql" level="OFF"/>
    <logger name="reactor.netty" level="OFF"/>
    <logger name="io.netty" level="OFF"/>
    <logger name="jakarta" level="OFF"/>

    <!-- ROOT: SOMENTE ARQUIVOS (SEM CONSOLE) -->
    <root level="OFF">
        <appender-ref ref="FILE_JSON"/>
        <appender-ref ref="FILE_LOG"/>
    </root>
</configuration>
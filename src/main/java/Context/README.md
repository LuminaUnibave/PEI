# Contexto de Sessoes

Esta pasta guarda notas em Markdown para orientar sessoes futuras sobre o backend do projeto Lumina.

Convencao:
- `phase-XX-*.md`: registros incrementais de contexto, decisoes, pareceres e proximas acoes.
- Cada phase deve citar a data, o escopo analisado e o estado verificado.

Escopo atual:
- O foco destas notas e somente o backend Java/Spring Boot.
- O banco de dados esta no Supabase.
- O `.env` local pode estar desatualizado.
- Backend e frontend ainda serao publicados em deploy.

